1) All the app will be migrate to Azure Container App
2) All JDK must be upgraded to 17
3) If there is messaging system is used in the project, migrate to azure service bus. using the AMQP solution
4) Using the keyvalut for the secret management, don't use MI or mount secret